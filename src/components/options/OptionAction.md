The OptionAction component is used to give an action button or an option button typically used in table rows or grid cells.

```js
	<OptionAction   showDetail={true}
                    showOption={true}
                    onDetail={()=>{}}
                    onOption={()=>{}} />
```